breakpoint-infix()
	


make-grid-columns($gutter = $grid-gutter-width)
	$grid-column
		position relative
		// width 100%
		// min-height 1px // Prevent columns from collapsing when empty
		// padding-right ($gutter / 2)
		// padding-left ($gutter / 2)
		
	for $breakpoint, $i in $grid-breakpoints
		$infix = keys($grid-breakpoints)
		.col-{$breakpoint}-{$i}
			@extend $grid-column
			
	// for $index in 1..2
	// 	.col-{$index}, .col-xs-{$index}, .col-ssm-{$index}, .col-sm-{$index}, .col-md-{$index}, .col-lg-{$index}
	// 		@extend $grid-column